version: "2"

# Default linters (errcheck, govet, ineffassign, staticcheck, unused)
# plus gofmt. No linter is disabled to make code pass.
linters:
  default: standard

formatters:
  enable:
    - gofmt
